<h2 class='page-header'><%= @quiz.name %></h2>
	<table class="table table-bordered">

<ol class='list-group'>
	<% @questions.each do |question| %>
		<li class='list-group-item'><%= question.content %><%= question.answers %><%= question.answer_id %></li>
	<% end %>
</ol>
</table>
