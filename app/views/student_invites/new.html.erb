<div class='row'>
	<div class='col-md-6 col-md-offset-3 well'>
		<%= form_for [@classroom, @student_invite] do |f| %>
			<%= f.label :email %>
			<%= f.email_field :email %>

			<%= f.submit %>
		<% end %>
	</div>
</div>


